# URL base del backend FastAPI. En dev local por defecto es http://localhost:8080.
# Para la demo dentro de Telegram, apunta a la URL pública del backend (o del túnel).
VITE_API_BASE_URL=http://localhost:8080
